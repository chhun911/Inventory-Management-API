# URL where the Vue dev/preview server is running
BASE_URL=http://localhost:5173

# URL of the running NestJS API (used for setup helpers, optional)
API_URL=http://localhost:3000/api

# Browser: chrome | firefox
BROWSER=chrome

# Set to "false" to see the browser run; default headless
HEADLESS=true
